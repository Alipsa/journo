<ruleset xmlns="https://www.mojohaus.org/VERSIONS/RULE/3.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://www.mojohaus.org/VERSIONS/RULE/3.0.0 https://www.mojohaus.org/versions/versions-model/xsd/rule-3.0.0.xsd"
         comparisonMethod="maven">
  <ignoreVersions>
    <ignoreVersion type="regex">.*-preview</ignoreVersion>
    <ignoreVersion type="regex">(?i).*[._+-](alpha|b|beta|rc|m|ea|pr).*</ignoreVersion>
    <ignoreVersion type="regex">.*jre6</ignoreVersion>
    <ignoreVersion type="regex">.*jre7</ignoreVersion>
    <ignoreVersion type="regex">.*jre8</ignoreVersion>
    <ignoreVersion type="regex">.*jre9</ignoreVersion>
    <ignoreVersion type="regex">.*jre10</ignoreVersion>
    <ignoreVersion type="regex">.*jre11</ignoreVersion>
    <ignoreVersion type="regex">.*jre12</ignoreVersion>
    <ignoreVersion type="regex">.*jre13</ignoreVersion>
    <ignoreVersion type="regex">.*jre14</ignoreVersion>
    <ignoreVersion type="regex">.*jre15</ignoreVersion>
    <ignoreVersion type="regex">.*jre16</ignoreVersion>
    <ignoreVersion type="regex">.*jre18</ignoreVersion>
    <ignoreVersion type="regex">.*jre19</ignoreVersion>
    <ignoreVersion type="regex">.*jre20</ignoreVersion>
    <ignoreVersion type="regex">.*jre22</ignoreVersion>
    <ignoreVersion type="regex">.*jre23</ignoreVersion>
    <ignoreVersion type="regex">.*jre24</ignoreVersion>
  </ignoreVersions>
</ruleset>